# our build should run within a Ruby box
box: wercker/python
build:
  steps:
    # Run a smart version of bundle install
    # which improves build execution time of
    # future builds
    - pip-install

    # A custom script step
    # that actually builds the jekyll site
    - script:
        name: generate site
        code: python cli_nalblog build